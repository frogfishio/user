{
  "name": "@frogfish/user",
  "version": "1.0.5",
  "description": "User Service for Frogfish engine",
  "main": "gulpfile.js",
  "scripts": {
    "test": "gulp prepare-debug; tsc; mocha --exit --recursive build/debug/test/account",
    "watch": "gulp prepare-debug; tsc -w",
    "build": "gulp build",
    "clean-build": "gulp clean; gulp build",
    "dist": "npm version patch; gulp clean; gulp build; cd build/release; npm publish; git push origin master --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frogfishio/user.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/frogfishio/user/issues"
  },
  "homepage": "https://github.com/frogfishio/user#readme",
  "dependencies": {
    "@frogfish/engine": "^1.0.24"
  },
  "devDependencies": {
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "mkdirp": "^0.5.1",
    "typescript": "^3.6.3"
  }
}
